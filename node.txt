目的:通过Node.js实现前后端完全交互

	1.获取数据(页面加载时/打开时,将后台数据写入页面),
		jax get,获取的数据:后台json中的所有数据,对应字段,注意(删除字段,需要id)

	2.添加功能 ajax add传值:用户输入的字段,后台接收前台传过来的info对象,读取json中所有的数据,

		fs.readFile()===>字符串===>对象(arr数组)===>info.id;添加到arr最前面
		fs.writeFile()===>JSON.stringify()字符串===>

		res.end()==JSON.stringify()字符串==>前台

		前台=ajax=>字符串==>对象==>渲染到页面

	3.删除功能 ajax del传值:要删除的id
		后台要接收前台传过来的info{id:"989999070998"};
		后台还需要从已存在的json中读取所有数据===>字符串==>对象(arr数组),
		用前台传过来的id与后台获取到的所有的arr的id进行对比,若匹配上,则从arr这个数组,删除该条数据,且用break停止循环
		将新的arr写入json.
		将新的arr传给前台=ajax=>字符串==>对象==>渲染到页面 